# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
/node_modules

# Build output
/dist
/dist-ssr

# Environment Variables
.env           # Excludes the .env file (Vite's default)
.env.* # Excludes any specific .env files like .env.development, .env.production, etc.
!*.env.example # IMPORTANT: If you have an example .env file you want to commit, keep this line.
               # Otherwise, remove it if you don't use .env.example.
*.local        # You already have this for .env.local, which is good.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?